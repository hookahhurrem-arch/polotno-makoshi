meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  skip: false
  additionalCommands: npm run build:amvera
  artifacts:
    "*": /
    ".*": /
    ".output": /

run:
  command: node .output/server/index.mjs
  containerPort: 80
