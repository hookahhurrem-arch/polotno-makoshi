meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  skip: false
  additionalCommands: test -f release/server/index.mjs
  artifacts:
    "*": /
    "release": /
    "index.js": /
    "package.json": /

run:
  scriptName: index.js
  containerPort: 80
